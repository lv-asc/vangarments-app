import EntityConfigurationPage from './EntityConfigurationClient';

export const metadata = {
    title: 'Entity Configuration - Admin',
};

export default function Page() {
    return <EntityConfigurationPage />;
}
